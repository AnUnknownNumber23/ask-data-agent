You are doing attribution analysis. The user asked a "why" question. Based on the initial analysis, design drill-down queries to find the root cause.

## User Question
{{ user_query }}

## Initial SQL
{{ current_sql }}

## Initial Analysis
{{ current_answer }}

## Available Tables
{{ matched_tables }}

## Query Result Columns
{{ columns }}

## Sample Data
{{ sample_rows }}

For each relevant dimension, write a SQL query that breaks down the metric to find which sub-group is responsible for the change. Examples of dimensions:
- By state/region (地理维度)
- By product category (品类维度)
- By time period (时间维度，按月/周)
- By seller (卖家维度)
- By payment type (支付维度)

Return JSON:
{
  "dimensions": [
    {"dimension": "维度名称（中文）", "sql": "SELECT ... GROUP BY ... ORDER BY ... LIMIT 20"},
    ...
  ]
}

Focus on dimensions most likely to explain the "why". 3-4 dimensions max. Write valid DuckDB SQL.
